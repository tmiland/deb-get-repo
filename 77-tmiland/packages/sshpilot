DEFVER=1
get_github_releases "mfat/sshpilot"
if [ "${ACTION}" != "prettylist" ]; then
  URL=$(grep -m 1 "browser_download_url.*\.deb\"" "${CACHE_FILE}" | cut -d'"' -f4)
  VERSION_PUBLISHED=$(cut -d'/' -f8 <<<"${URL}")
fi
PRETTY_NAME="sshPilot"
WEBSITE="https://github.com/mfat/sshpilot"
SUMMARY="Simple, user-friendly SSH connection manager."
